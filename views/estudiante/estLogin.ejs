<%- include("../header"); %>
    <h1>Login</h1>

    <ul>
      <% if (typeof errors != 'undefined') { %> <% errors.forEach(error => { %>
      <li><%= error.message %></li>
      <% }) %> <% } %>
    </ul>

    <form action="/estudiante/estLogin" method="POST">
      <div>
        <input
          type="text"
          id="codigo"
          name="codigo"
          placeholder="Numero de Registro"
          required
        />
      </div>
      <div>
        <input
          type="password"
          id="password"
          name="password"
          placeholder="Password"
          required
        />
      </div>
      <div>
        <input type="submit" value="Login" />
      </div>

      <a href="/estudiante/estRegistro">Register</a>
    </form>
<%- include("../footer"); %>